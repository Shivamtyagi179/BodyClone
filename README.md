# BodyClone
🧠 Body Clone using OpenCV &amp; Python  A real-time computer vision project that captures human movements through a webcam and replicates them as a “body clone.” Built using Python and OpenCV, this system detects and tracks body actions, enabling the clone to mirror gestures and movements with minimal latency.
